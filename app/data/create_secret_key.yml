secret_key_all_read:
  secret_key_name: ~characters(character_count=10)
  permission: Read

secret_key_all_write:
  secret_key_name: ~characters(character_count=10)
  permission: Write

secret_key_all_none:
  secret_key_name: ~characters(character_count=10)
  permission: None

secret_key_update:
  permission: Write